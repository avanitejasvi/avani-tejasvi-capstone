# Build Log

| Date | Time Spent | Rough Tokens Used | What Shipped |
| --- | --- | --- | --- |
| 2026-09-11 | 10 min | ~5k | Set up BUILD_LOG.md to track session-by-session progress (date, time spent, token usage, shipped work) going forward. |
| 2026-09-17 | ~1h50m | ~40k (est.) | Built the Menu Preference Matching skill end-to-end: wrote `skills/skills.md` (Drive→match→Calendar workflow spec, data models, open questions resolved), `agent.json` for the meal-match-agent, and a 648-line `react_agent.py` ReAct-style agent (Pydantic models, FilesystemTool, GoogleDriveSkill for reading the live "meal-menu-match" weekly grid sheet, GoogleCalendarSkill for event create/RSVP-check/delete, MenuPreferenceMatchingSkill scoring logic with tag-weight fallback and decline feedback). Added `authorize_google.py` OAuth helper, sample data (`menu_archive_sample.csv`, `preferences_u001.json`), and `requirements.txt`. Wired up real Google OAuth client ID/secret, refresh token, and a Gemini API key in `.env`. **Security note:** `.env` has no `.gitignore` and was committed with live secrets in commit `b8dd8e1`, already pushed to the public GitHub repo — these credentials should be treated as compromised, rotated, and `.env` should be removed from tracking (see chat for details). |
| 2026-09-17 | long session (exact duration n/a) | n/a (heavy tool-call session, no exact count) | Got the ReAct meal-matching agent working live end-to-end against real Google Drive/Calendar (separate Claude Code session from the one above). Validated scoring logic locally first (lunch/breakfast/decline scenarios all passed). Ran `authorize_google.py` with the user to mint `GOOGLE_REFRESH_TOKEN` into `.env`. First live Calendar write succeeded immediately; live Drive read failed because the real Drive folder was named "meal-match-menu" (word order swapped from the code's expected "meal-menu-match") — user renamed the folder to match. After that, discovered the real spreadsheet is a weekly grid (days as columns via a "Date" header row; dish categories grouped under Breakfast/Lunch/Evening Snacks/Dinner section headers, with two separate "Lunch" venue blocks and no explicit tags column) — a completely different shape from the flat table `skills.md`/original code assumed. Rewrote `GoogleDriveSkill.read_menu_sheet` in `react_agent.py`: added `_find_day_column` and `_parse_weekly_grid` helpers, `GRID_SECTION_HEADERS`/`GRID_EMPTY_MARKERS` constants, and threaded `event_date` through `act_perceive_menu`/`run` to parse the real grid and derive tags from category labels (e.g. "Gravy Veg" → `["gravy","veg"]`). Verified live for lunch (2026-09-15) and dinner (2026-09-17): both pulled real dishes, scored them, created real Calendar invites, and grew `known_dishes` from 5→17 entries via the self-improvement loop. **Open follow-ups (not yet done):** `skills/skills.md` is now stale — it documents the old flat schema and lists two "open questions" as unresolved that are actually already resolved in code. Also, non-canonical section-header rows in the grid (e.g. venue-specific headers like "Rice Bowl Concept only in Rasoi Dining") get folded into whatever meal slot preceded them instead of being recognized as their own section — harmless today (empty cell that day) but could misfile a dish on a different day. |
| 2026-09-18 | ~20 min | ~15k (est.) | Design-review session, no code changed. User shared/iterated on a written workflow spec for turning the meal agent into a recurring daily loop (9:30 AM Drive menu read → Gemini extracts items → match item-level preferences → check mess timings + Calendar for clashes → schedule with user as invitee → compact event description → user replies YES/NO/MAYBE + optional note → Gemini interprets feedback → update that item's preference → repeat). Reviewed it against the actual repo state and flagged two concrete gaps before implementation: (1) `agent/data/preferences_u001.json` doesn't support the proposed score+times-eaten+"unknown" model yet — it currently appends a new `{name, tags, rating, last_seen}` entry per sighting with visible duplicates (e.g. "Paneer Kadai" logged twice) and no count/unknown-state field, so an upsert-by-name schema change is needed first; (2) the "how does the user respond" question (email reply vs. Calendar RSVP) is still open — recommended email as primary since MAYBE + freeform notes (e.g. "only if there are no other options") can't ride on an RSVP alone. No implementation started; waiting on user to confirm direction before touching the schema or interaction channel. |
| 2026-09-18 | ~10 min | ~5k (est.) | Comparison session, no code changed. Compared `oldskills.md` (Sheet-based menu read, known_dishes/tag_weights cold-start scoring, binary accept/decline feedback, no calendar-conflict check) against `newskills.md` (image-OCR menu read, score+times-eaten "unknown" preference model, YES/NO/MAYBE+note feedback loop, calendar-conflict/alternate-slot check) against the actual repo state and each doc's own flagged open questions. Verdict given to user: neither wins outright — recommended taking old's more reliable pipeline (structured Sheet read, tag-weight cold-start estimate) and grafting in new's two missing features (the richer feedback loop, which fixes a gap old itself flags; and the calendar-conflict check, which old doesn't have at all). No decision made yet on which direction to actually build. |
| 2026-09-18 | ~1h (est.) | ~90k (est.) | User committed to the "new" (Gemini image-intake) direction and rewrote `skills/skills.md` accordingly; rebuilt `react_agent.py` end-to-end to match it. Migrated `preferences_u001.json` from `{rating}` to `{score, times_eaten, last_response, last_note, last_seen}` and deduped 8 repeated entries (38→30). Resolved and wrote all 5 Open Questions directly into `skills.md` (0.85 fuzzy threshold verified against real dish names, concrete score deltas, conflict-flag-in-Calendar fallback, no_data handling kept, first-real-feedback-replaces-estimate). Added `GeminiSkill` (image→dish names, name→tags inference since the photo only yields names, note-sentiment classification) via the current `google-genai` SDK (added to `requirements.txt`, installed), each with an offline/naive fallback so the whole loop runs with zero API calls. Added calendar-conflict detection + alternate-slot search over a fixed mess-timing window, flagging (not silently dropping) the meal if no slot is free. Rewrote `agent.json` — it was previously unused by the code and explicitly forbade image processing/calendar sync, directly contradicting the new design; now points at `gemini-2.5-flash` and describes the three actual Gemini call sites. Added a `--live` CLI flag (default: dry run against the local `menu_intake_sample.json` fixture, no Drive/Gemini/Calendar calls) specifically so repeated test runs can't create duplicate real Calendar events again. Verified the matching/scoring/fuzzy-match/feedback logic against `skills.md`'s own sample table via a standalone script that never touches the real preferences file or a live Calendar; did not run a live end-to-end trial. **Not done:** no real photographed menu image exists in Drive yet (the folder still only has the old weekly-grid spreadsheet), so a live `--live` run will fall back to the fixture until the user uploads one; `STUDENT_EMAIL` is unset so live RSVP polling isn't wired up yet either. |
| 2026-09-18 | n/a (direct file edits, not a code session) | n/a | User revealed the campus actually runs **two separate messes (Rasoi, Aahar) with genuinely different menu structures** — Rasoi is rice-bowl style, Aahar is full buffet — and hand-edited the data files to match, ahead of code catching up. New `mess_structure.json` (created earlier this session as a placeholder) was rewritten into the real spec: per-meal, per-mess category label lists Gemini should scan for (Rasoi lunch: Salad/Starter/Bowl/Accompaniment/Dessert/Beverage; Aahar lunch and dinner: full buffet categories including separate Jain veg/non-Jain veg lines), plus a fortnightly `sunday_brunch` special slot that replaces breakfast+lunch and deliberately leaves its category list `null` since that menu shape varies week to week. `mess_timings.json` updated to the real windows (breakfast 7:30-9:30, lunch 11:30-15:00, evening snacks 17:00-18:00, dinner 19:30-22:30) plus the same fortnightly `sunday_brunch` slot. `preferences_u001.json` fully replaced with a real 32-dish rated set (fruits, bread, breakfast items, desserts, milk, staples) with `tag_weights` recomputed and verified by hand against it, `dietary_restrictions`/`skip_meal_slots` reset to empty, and an explanatory comment documenting the exact-match-vs-tag-fallback reasoning (e.g. why `dal` nets 4.5 despite one neutral entry, why `gravy`/`fried` land only mildly positive). `skills.md` retitled "Meal Recommendation Agent (Build Version)" and updated to match: added `ExtractedDish` (tags each extraction with mess/meal_slot/category), an OCR fuzzy-match safeguard section for Gemini misreads (e.g. "Paneer Tkka"), and two new unresolved Open Questions — **which mess gets scored when both Rasoi and Aahar serve the same slot**, and **how "fortnightly" Sunday brunch gets anchored to actual calendar dates** (no reference start date defined yet). **Not done:** breakfast's per-mess category list in `mess_structure.json` is still `null`/unconfirmed; neither new Open Question has an answer yet; `react_agent.py` has not been updated to read the mess/multi-venue structure — it still scores against a single flat mess. |
| 2026-09-18 | ~15 min | ~10k (est.) | Data-only session: user pasted a hand-written, richer preference spec (v2.1 style — pairing rules, explicit liked/disliked combinations, learned-from-both-weeks summary, menu-inference likelihood lists, scoring priority/weights, meal-balance/repetition/exploration policy, confidence tiers, output rules) and asked to merge it into `preferences_u001.json`. Read `react_agent.py`'s `UserPreferences` Pydantic model first to check compatibility. Merged programmatically (script run, not hand-edited): enriched tags on 11 existing `known_dishes` entries (e.g. `Chana` +`chickpea`, `Fryums` +`fried`/`snack`), added 3 new dishes (`Garlic Crostini`, `Bread Sticks`, `Vegetable Pulao`), and — critically — preserved the live-tracked `rating` for any dish with real eaten history (`times_eaten > 0`) rather than overwriting with the pasted static rating (e.g. `Slice Cake` stayed at `1.5`/`"no"` instead of the pasted `5`). Added the new sections as top-level fields (`version`, `rating_scale`, `explicit_combinations`, `pairing_rules`, `learned_from_both_weeks`, `menu_inference_rules`, `scoring`, `meal_balance`, `repetition`, `exploration`, `confidence`, `output_rules`), plus the pasted `tag_weights` policy block renamed to `tag_weights_policy` (kept separate from the real `tag_weights: {}` dict the code recomputes each run, since the pasted shape doesn't match `dict[str, float]` and would break parsing if merged in directly). Verified the result loads cleanly through the real `UserPreferences(**data)` constructor (166 known dishes). **Not done / flagged to user:** `UserPreferences` has no fields for the new sections beyond `known_dishes`, so `act_persist_preferences` will silently drop `version`/`pairing_rules`/`scoring`/etc. the next time the agent runs **live** (dry runs don't write back) — those rules currently have no effect on scoring and won't survive a live persist until the model is extended to store and use them; that extension was explicitly not done this session. |
| 2026-09-18/19 | ~2h (est.) | ~150k (est.) | Live-trial session (separate Claude Code session from the two above, did not see the v2.1-merge warning until the very end). Fixed a real bug: `gemini-2.5-flash` was deprecated mid-session (404) → moved default model to `gemini-3.6-flash`, which then proved to have persistent `503` capacity issues → settled on `gemini-3.5-flash-lite`, and added `max_output_tokens=16000` since the real board (~360 structured dishes across every mess/meal) was silently truncating mid-JSON on the old default. Found and fixed the actual root cause of an unusable first batch run: the photographed board is a **weekly rotating menu** (one column per weekday) but extraction wasn't capturing which day each item belonged to, so `meal_slot` alone flattened 6+ days into one bucket (132 "lunch" items instead of ~22) — added a `day` field to `ExtractedDish`, updated the extraction prompt to require it, and filter on `meal_slot` + `event_date`'s weekday together. Refactored feedback targeting: `act_apply_feedback` previously applied a single YES/NO/MAYBE to *every* eat-decision dish in a run (could be dozens); now `ScheduledMeal.top_picks` records exactly the (compact, top-3, mess-labeled) dishes actually named in the event description, and only those get updated — added `MenuPreferenceMatchingSkill.select_top_picks`. Added `cached_items` passthrough on `act_menu_intake`/`run()` so a multi-day/multi-slot batch reuses one Gemini extraction instead of one per call (cheaper, avoids free-tier rate limits, avoids day-to-day inconsistency from a transient failure). Ran real batches live: deleted and correctly re-scheduled 16 events (Sept 15-18, all 4 meal slots) after the day-scoping fix, pulled real YES/NO/MAYBE + RSVP-comment feedback for 12 of them via `GoogleCalendarSkill.get_response` (now returns `(response, note)`, reading the attendee's real `comment` field) and applied it — verified via a full before/after replay script. Scheduled the full weekend (Sat 09-19 breakfast/lunch/evening_snacks/dinner, Sun 09-20 sunday_brunch/evening_snacks/dinner after confirming with the user it's a brunch weekend) live, real conflicts correctly detected (a 5hr Saturday workshop, an overnight "OUT OF CAMPUS" event) and flagged rather than silently scheduled into. **Data-loss found and partially recovered:** discovered mid-session that the previous session's v2.1 preference merge (pairing rules, combinations, scoring/meal-balance/repetition/exploration policy — see prior row) had been silently stripped by the very first live `act_persist_preferences` write, exactly as that session warned it would be, well before this session started. The full pasted spec was recovered from a stale scratchpad file from that other session (`.../7ba00a85.../scratchpad/pasted_prefs.json`, not durable) and preserved into the repo as `agent/data/preferences_u001_extended_spec_recovered.json`. **Not done:** that extended schema is still not wired into `UserPreferences`/matching — `preferences_u001.json` currently only has `known_dishes`/`tag_weights`/`comment`/`tag_weights_note`; pairing rules, combinations, meal-balance/repetition/exploration policy have no effect on scoring yet. Also unresolved: several NO responses this session carried notes like "didn't eat at mess at all" (absence, not dislike) but were scored identically to a real decline — worth a distinct handling later. 4 of the Sept 15-18 breakfast events and all 6 weekend events are still pending real responses as of this log entry. |
| 2026-09-19 | ~1h (est.) | ~110k (est.) | Planning-only session, no code changed. Scoped turning `meal-match-agent` from a single-user CLI (`user_id="u001"` hardcoded, one refresh token in `.env`, one Drive account, one `preferences_u001.json`) into a multi-tenant app hosted on Railway with per-user Google sign-in. Clarified with the user: audience is all of FLAME (`@flame.edu.in`-gated, staying in Google OAuth "Testing" mode for now — full verification is out of scope), menu images move from Drive to a plain in-app upload (any logged-in user uploads the week's photo, Gemini extracts it once, result shared across everyone), and preferences move from per-user JSON files to Railway Postgres (JSONB columns, close to today's shape). Read `react_agent.py` in full via a research pass: confirmed `user_id` is already a constructor param on `ReActMealAgent` (only `main()` hardcodes `"u001"`) and `MenuPreferenceMatchingSkill` is already pure/stateless — both reusable as-is; identified `GoogleDriveMenuImageSkill`/`DRIVE_FOLDER_NAME`/the Drive scope as fully removable, and the shared `menu_intake_<date>.json`'s `processed`-flag mutation as a live race condition once concurrent users exist. Drafted and got sign-off on a full plan (FastAPI web layer, 5-table Postgres schema, per-user web OAuth flow replacing `authorize_google.py`'s desktop `InstalledAppFlow`, Fernet-encrypted refresh tokens, Railway Cron job for the daily run, 4-phase build order) — saved at `~/.claude/plans/humming-pondering-thimble.md`. **Immediately after approval, the user sent a large batch of amendments** (OAuth tokens actually expire in 7 days under "Testing" mode — need "In production" publishing instead; RSVP-based feedback learning doesn't work once the single `STUDENT_EMAIL` invitee is removed — owner's own RSVP is always "accepted"; new users need a `/settings` page for dietary restrictions since nothing sets those today; menu upload needs a preview-then-confirm step and file-type/size validation; a crash-window duplicate-event risk in the scheduling idempotency logic; drop the two-cron-service split in favor of one `run_all.py`; **switch from daily to weekly** — `daily_run.py` → `weekly_run.py`, Monday 9:30am IST cron, plus a "no menu uploaded yet" reminder-event + dashboard-banner + catch-up-scheduling flow for late uploads; timezone handling needs `Asia/Kolkata` explicitly everywhere; drop the Railway Volume entirely (hash the upload instead of storing the image); OAuth callback hardening (`email_verified`+`hd` claim check, PKCE, `is_active` check); preferences read needs `SELECT ... FOR UPDATE`; treat the already-committed secrets as burned and rotate). **Not done yet:** none of this has been merged into the plan file or built — the amendment message was cut off mid-send (right after "EXTRA VERIFICATION- User unticks Calendar") and a second, separate amendment (the weekly-schedule rewrite) arrived before confirming whether the first list was complete. Waiting on the user to confirm (1) whether anything was lost in that cutoff and (2) that both amendment batches should be merged into one updated plan before any implementation starts. |
| 2026-09-19 | ~2h (est.) | ~230k (est.) | Same-day follow-up session: merged both amendment batches into a v2 plan (re-saved at the same plan path) and, once approved, built essentially all of it — Phases 1-3 of the 4-phase build order. **New files:** `agent/config.py`, `agent/timezone.py` (`Asia/Kolkata` helpers, no bare `date.today()`/`datetime.now()` anywhere in the new code), `agent/crypto.py` (`MultiFernet`-wrapped token encryption), `agent/db.py` + `agent/models_db.py` (SQLAlchemy, 6 tables: `users`, `oauth_tokens`, `preferences`, `menu_intake`, `pending_menu_uploads`, `scheduled_meals`), `agent/repository.py` (the sole Postgres access point — claim-row idempotency via `INSERT ... ON CONFLICT`, `SELECT ... FOR UPDATE` on preference writes, `MultiFernet` token encrypt/decrypt), `agent/migrate_json_to_db.py`, `alembic.ini` + `agent/alembic/` (hand-written initial migration for all 6 tables), `agent/web/` (FastAPI app: `/auth/login`+`/auth/callback` with PKCE/state/`hd`+`email_verified`+granted-scope checks, `/settings`, the two-step `/menu/upload` → preview → `/menu/upload/confirm`, a minimal dashboard with reauth/no-menu banners, 4 Jinja2 templates), `agent/jobs/` (`scheduling.py`'s `schedule_week()` shared by the Monday cron and the post-confirm catch-up trigger; `weekly_run.py`; `collect_feedback.py` — event-exists-or-cancelled is now the only feedback signal, no RSVP/note sentiment; `run_all.py`), `railway.toml`. **Rewrote `agent/react_agent.py`:** removed `GoogleDriveMenuImageSkill`/`DRIVE_FOLDER_NAME`/the Drive scope/`STUDENT_EMAIL` entirely; `GoogleCalendarSkill` now takes per-user `Credentials` and builds its service once instead of every method rebuilding one from global env vars; added `deterministic_event_id` (sha256 hex is already valid Calendar-id charset — no remapping needed) and `create_event_with_id`/`create_reminder_event` (409 = already-created, not a duplicate) plus `get_event_status`; `ReActMealAgent` takes a `repo: AgentRepository` (a `Protocol`, not an import, to avoid a circular dependency with `repository.py`) instead of file paths; deleted `act_collect_response`/`act_apply_feedback`/`act_mark_intake_processed` (genuinely dead once RSVP-based feedback moved to `collect_feedback.py`'s different signal) and the `NAIVE_POSITIVE_WORDS`/`NAIVE_NEGATIVE_WORDS`/`interpret_note_sentiment_naive` fallback that only they used; added `FileRepository` so `main()`'s local dry-run testing still works against the `agent/data/*.json` fixtures with zero DB/Google setup, preserving the project's long-standing test-before-live workflow. **Verified without touching real tracked data:** `pip install -r agent/requirements.txt` succeeds; every new/changed module imports cleanly; a full dry-run CLI invocation against a throwaway `preferences_testuser.json` (created and deleted within this session, never `preferences_u001.json`) round-tripped correctly through fixture intake → filter/score → register-new-dishes → `FileRepository.save_preferences` → dry-run schedule; `crypto.py` round-trips a token; `deterministic_event_id` produces a stable, Calendar-id-charset-valid hash; `week_start_ist` correctly snaps a Saturday back to that week's Monday. **Not done / explicitly deferred to the user:** Phase 4 (actually provisioning the 3 Railway services + env vars + Volume-free deploy, switching the Google Cloud OAuth Client to Web-application type and "In production" publishing, adding the real redirect URI) — needs real console/dashboard access this session doesn't have. No live Postgres was available to test the Alembic migration or the `ON CONFLICT`/`FOR UPDATE` SQL against a real server. The committed `.env` secrets are still not rotated. `skills/skills.md` and `agent.json` still describe the old single-user Drive-based flow and haven't been updated to match. |
| 2026-09-19 | ~45 min (est.) | ~60k (est.) | Same-day follow-up: gave a step-by-step Railway/Google-Console setup walkthrough (Web-application OAuth client, consent screen → "In production", the 3 Railway services + env var table + domain-first ordering, running the Alembic migration and `migrate_json_to_db.py` against Railway's Postgres) — not yet executed, needs the user's actual dashboard access. Then audited the multi-tenant rebuild against `plan.md` §7's 5 agentic criteria before touching docs, per explicit request. Found a real regression, not just a documentation gap: removing the single `STUDENT_EMAIL` attendee (correct, required for multi-tenancy) left `collect_feedback.py` able to detect *only* a cancelled event → decline; nothing in production could ever raise a `known_dish` rating from real experience anymore, since nothing called `apply_feedback` with a "yes". That silently breaks §7's "it updates itself" criterion in the positive direction and was quietly re-orphaning `GeminiSkill.interpret_feedback_note` (zero callers). Fixed by adding a manual feedback path: `Repository.get_scheduled_meal`/`list_pending_manual_feedback` (new), `agent/web/routes_feedback.py` (`GET /feedback` lists a student's own past unrated meals, `POST /feedback/<id>` applies yes/no/maybe + optional note through the *same* `MenuPreferenceMatchingSkill.apply_feedback` and `GeminiSkill.interpret_feedback_note` the single-user build always used — restoring the rubric's second live Gemini call site too), `feedback.html`, and a dashboard banner/nav link showing the pending count. Reuses the existing `scheduled_meals.feedback_applied` flag rather than adding a second one, so a manually-rated meal is automatically skipped by the next automated `collect_feedback.py` pass. Rewrote `agent/skills/skills.md` end to end to match the actual current architecture (upload-based shared menu, weekly cadence, per-user Calendar, the two-channel feedback design and *why* it's asymmetric) and added an explicit closing section mapping all 5 of `plan.md` §7's criteria to the current code, stating plainly where criterion 5 nearly broke and how `/feedback` restores it — traceability the user asked for directly. Corrected two small pre-existing staleness bugs in `agent/agent.json` noticed while doing this pass: `model` field still said the deprecated `gemini-3.6-flash` instead of the actual runtime default `gemini-3.5-flash-lite`, and the extraction JSON shape description was missing the `day` field. **Verified:** all new/changed modules import cleanly (same dummy-env-var method as the prior row); JSON-validated `agent.json`. **Not done:** the new `/feedback` flow hasn't been exercised against a live Postgres/Calendar (same limitation as before — no live server available here); `skills.md`'s new content hasn't been reviewed by the user yet. |
| 2026-09-19 | ~30 min (est.) | ~40k (est.) | Same-day follow-up, mid-Railway/Google-Console-walkthrough: user asked to let students bring their own AI API key rather than relying solely on the shared `GEMINI_API_KEY`. Clarified scope first (any of Anthropic/OpenAI/Groq/Gemini; falls back to the shared Gemini key if unset; applies only to a student's own `/feedback` sentiment call, not the shared weekly menu-photo extraction, since that's a shared action needing vision + the app's own prompt, not a personal text-classification call). Added `agent/llm_providers.py` (`classify_note_sentiment(provider, api_key, ...)` — one shared prompt/parse, dispatched per provider; Groq reuses the `openai` SDK pointed at Groq's OpenAI-compatible endpoint rather than needing a fourth SDK), a new `user_llm_keys` table (`agent/models_db.py` + `agent/alembic/versions/0002_user_llm_keys.py`) storing one encrypted key + provider per student (reusing `crypto.py`'s existing `encrypt_token`/`decrypt_token` rather than adding parallel encryption code), `Repository.save_llm_key`/`get_llm_key`/`has_llm_key`/`delete_llm_key`, and wired it into `routes_feedback.py` (own key if set, else the existing shared-`GeminiSkill` path, unchanged) and `/settings` (provider dropdown + key field + a "remove my saved key" checkbox — the key itself is never re-displayed once saved, only which provider is active). Added `anthropic`/`openai` to `requirements.txt`. **Verified:** both new SDKs install cleanly in the test venv; all changed/new modules import; `classify_note_sentiment`'s prompt-building and sentiment-word parsing (including correctly rejecting an invalid word) tested directly. **Not done:** no real API key from any of the 4 providers was exercised against a live endpoint here (would cost real money/quota to test blind) — first real use should be a manual `/feedback` submission with a real key once the app is actually deployed. |
| 2026-09-19 | ~1h (est.) | ~50k (est.) | Same-day follow-up: walked the user through Railway/Google Console setup live (Web-application OAuth client, redirect URI, consent screen → "In production", Railway project + Postgres + web service + domain, env vars, cron service) — installed the Railway CLI locally (`npm` global install hit an `EACCES` permission error, switched to the official `curl | sh` installer, which worked) to help diagnose once both services showed `● Failed`. Root-caused it via `railway logs --since 1d`: Railway's build tool ("Railpack") only saw `BUILD_LOG.md`/`README.md`/`plan.md` at the repo root — because the actual work has lived all day on `assessment-3-avani-tejasvi`, a branch that had **never been pushed to GitHub**, while Railway's connected service deploys from `main` (6 commits behind, effectively pre-agent-build). Also discovered the local `origin` remote (`AvaniTejasvi_Capstone_DESG319`) and the repo Railway is wired to (`avani-tejasvi-capstone`) are the same repo — GitHub confirmed a rename — so no reconnection is needed, just changing Railway's deploy branch (flagged as the user's next action). Committed today's ~46 files (deliberately excluding `.env`, per the standing rotation flag) and attempted to push, which GitHub's **push protection** correctly blocked: a real Google OAuth Client ID/Secret, a GCP API key, and a refresh token are committed in plaintext in `.env` across **two** separate historical commits (`b8dd8e1`, `96b3590`, plus one push protection found and the initial local search missed: `f8314bab...`) — confirmed via `git branch -r --contains <sha>` after a fresh fetch that none of this had ever actually reached GitHub before now, on any branch. Rather than clicking GitHub's "allow this secret" override (this repo is public — that would have genuinely published live credentials), installed `git-filter-repo` (`pip3 install --user`) and ran `git filter-repo --path .env --invert-paths --force` after backing up `.env`'s working-tree content to the scratchpad first — this strips `.env` from **all** local branches' history in one pass while preserving every other commit's message/diff/order, at the cost of new commit hashes from `b8dd8e1` onward everywhere (unavoidable, and safe since nothing had been pushed). Confirmed filter-repo's known side effects handled correctly: it removed the `origin` remote (re-added, then pointed at the current repo name to drop the "repository moved" redirect) and deleted `.env` from the working directory entirely on its final checkout (restored from the pre-run backup immediately). Investigated an unexpected extra commit that appeared above the rewritten history (`d2a66f7`) — turned out to be harmless, made by the user directly in their own terminal (`git add -A && git commit`), containing only two accidentally-tracked `.DS_Store` files, zero real code. Added a `.gitignore` (`.env`, `.DS_Store`, `__pycache__/`, venvs — none of this existed before) and `git rm --cached` on the two `.DS_Store` files. Re-pushed `assessment-3-avani-tejasvi` — went through cleanly this time, no push-protection hits. **Verified:** `git log --all --oneline -- .env` empty post-filter-repo; `.env` restored and present on disk with its original content; the new commit is live on GitHub (`git push` returned "new branch" success, no rejection). **Not done:** Railway's service source still points at `main` — the user still needs to change its deploy branch to `assessment-3-avani-tejasvi` in the dashboard before any of today's real code actually gets built; `main`/`assessment-2-avani-tejasvi`/`refined-plan` all now have different (rewritten) local commit hashes than whatever, if anything, matches their own remote tracking state — worth a sanity check before anyone pushes those branches later. The actual Gemini/Google OAuth secret rotation this whole thread has been flagging still hasn't happened. |
| 2026-09-19 | ~2h (est.) | ~180k (est.) | Same-day follow-up: got the app fully live end-to-end, then improved the preference intake. User pointed Railway's service source at `assessment-3-avani-tejasvi`, which surfaced a real bug independent of the branch mixup: `Build Failed: dockerfile parse error on line 11: ENV names can not be blank`. Root-caused across several attempts — first tried an explicit `nixpacks.toml` (setup/install/start phases) since Nixpacks' own auto-detection was guessing wrong (requirements.txt isn't at repo root); hit the identical error one line later, meaning it wasn't our config's fault. Also caught Railway silently switching its actual builder between "nixpacks-v1.41.0" and "railpack-v0.39.0" across attempts depending on whether `railway.toml`'s `[build] builder = "NIXPACKS"` pin was present (removing it to avoid "duplicating" build steps broke it — Railpack, Railway's newer default, doesn't read `nixpacks.toml` at all and fails with an unrelated-looking "could not determine how to build the app"). Settled it by switching to a hand-written `Dockerfile` at the repo root, bypassing both auto-build systems entirely. That deployed and started ("Uvicorn running on ...") but every request 502'd with zero access-log activity — traced to the domain's target port being stuck at a placeholder value of `1` (`railway domain status`), fixed via `railway domain update ... --port 8080` once `EXPOSE 8080` was added to the Dockerfile to match. Next runtime crash: `KeyError: 'GOOGLE_CLIENT_ID'` even though `railway variables` showed it set — `railway run env` revealed the actual stored variable NAME had two leading spaces (`"  GOOGLE_CLIENT_ID"`), almost certainly a copy-paste artifact; this was very likely the same root cause as the earlier Nixpacks "ENV names can not be blank" bug, retroactively explaining it. Fixed by having the user delete and re-add the variable cleanly in the dashboard. Ran the Alembic migration and `migrate_json_to_db.py` against the live Railway Postgres via `railway ssh` (needed an SSH key registered first — `ssh-keygen` + `railway ssh keys add`, then `ssh-keyscan` to pre-accept `ssh.railway.com`'s host key since a non-interactive session can't answer the first-connection prompt) — both ran clean, all 6 tables created, all 163 real `known_dishes` migrated to `student.design15@flame.edu.in`. Confirmed the whole OAuth flow live end-to-end (real Google consent screen, real callback, landed on `/settings`). Next real bug found by actually using it: uploading a real menu photo threw a 500 (`json.decoder.JSONDecodeError: Unterminated string`) — the real board exceeded even the 16000-token ceiling set for the single-user build months ago. Raised it to 32000 and, more importantly, added `_parse_json_array_lenient` in `react_agent.py` so a still-too-large board salvages every complete dish up to the truncation point instead of failing the whole upload — tested against a synthetic truncated string before shipping. **Then, a separate, larger request:** rebuilt the `/settings` preference intake from two generic fields into 15 menu-grounded diagnostic questions (`agent/preference_questions.py`) — see the message-by-message design writeup for the full reasoning; the short version is every answer maps into the *existing* `UserPreferences`/`KnownDish` shape (seeded ratings, `dietary_restrictions`, or a `comment` note) with zero schema or learning-logic changes, grounded in the real 363-item two-week board data, and explicitly guards against overwriting a dish that already has real eaten history (`times_eaten > 0`) — the exact failure mode already logged once before in this project's history (2026-09-18/19 entry, the v2.1 merge). **Verified:** every fix confirmed against the real live deployment, not just locally — Railway status/logs/domain checked directly via CLI after each change; the preference-questions logic unit-tested with a fake form (correct dishes/ratings/tags seeded, correct restriction added, correct comment built, real-history dish confirmed untouched) before committing. **Not done:** the `cron` service (`poetic-beauty`) still shows `Failed` — likely needs the same Dockerfile/port fixes applied to it specifically, not yet checked; secret rotation still hasn't happened. |
| 2026-09-20 | ~2.5h (est.) | ~220k (est.) | Same live app, three more real requests. **(1) Restructured the account pages:** split the old single `/settings` into `/preferences` (the menu-grounded questions, dietary restrictions, skip-slots — now explicitly framed as a "baseline profile," and what a first-time login lands on instead of `/settings`) and a leaner `/settings` (bring-your-own AI key + a new **delete-account** action). Account deletion calls Google's `/revoke` endpoint on the stored refresh token (best-effort, network failure doesn't block the rest), then `Repository.delete_user` removes every personal row (`oauth_tokens`, `preferences`, `scheduled_meals`, `pending_menu_uploads`, `user_llm_keys`, the `users` row) while clearing (not deleting) `menu_intake.uploaded_by` since that's shared board data, not personal — guarded by a client-side confirm on the delete button. **(2) Added a three-layer preference model** per explicit spec: Baseline (`/preferences`, stable, unchanged) / Learned (`known_dishes.rating`/`times_eaten`, already existed) / Weekly Check-in (new `/checkin` route) — the check-in reads the *actual current week's* uploaded `menu_intake` (not the fixed historical sample `/preferences` uses), picks a handful of genuinely uncertain dishes by reusing `MenuPreferenceMatchingSkill.match_dish` directly, and routes every answer through the same `apply_feedback` the manual `/feedback` page already used — real experience layered on top of baseline, never overwriting it. Filled two gaps in the baseline set too (rice-vs-roti, a routine-timing free-text note). **(3) Walked back an earlier design assumption, and said so plainly:** during the multi-tenant rebuild, assumed a calendar owner's own RSVP couldn't carry a signal (event you merely own = auto-accepted), so the automated feedback channel was left decline-only. User pushed back, asking for the email-on-the-invite mechanism the project used before. Re-checking that assumption against this project's *own* history (`BUILD_LOG.md`'s 2026-09-18/19 entry) showed real YES/NO/MAYBE + RSVP-comment feedback had genuinely worked in the single-user build — because the student was a real **attendee**, not just the calendar owner, and an attendee entry starts `needsAction` regardless of whose calendar it's on. The "no signal" framing baked into `collect_feedback.py`'s and `get_event_status`'s docstrings from the previous day's rebuild was simply wrong, not a limitation of multi-tenancy at all. Fixed by giving `create_event_with_id` an `attendee_email` param (real `attendees[]` entry + `sendUpdates="all"`), restoring `get_response` (real `responseStatus` + RSVP comment), and rewriting `collect_feedback.py` to use it as the primary signal — cancelled → decline, real response → applied, still-pending → left unresolved for a later run rather than guessed at. Factored the "use the student's own AI key, else the shared Gemini key" sentiment logic out of `routes_feedback.py` into `agent/feedback_sentiment.py` so `collect_feedback.py` reuses it identically instead of duplicating it. Updated `skills.md`'s feedback section and its "What Makes This Still Agentic" §5 entry to describe three complementary channels instead of one decline-only automated check plus a manual workaround, narrating the reconsideration honestly rather than pretending the original design was right all along. **Verified:** all three changes confirmed live (routes return correct redirects/status codes, clean startup logs after each push); `get_response`'s attendee-matching and `create_event_with_id`'s attendee/`sendUpdates` wiring unit-tested with mocked Calendar responses (accepted/tentative/needsAction/cancelled/no-attendee cases) before shipping, not just trusted from memory of the old code. **Not done:** the `cron` service is still unchecked; secret rotation still hasn't happened; no real student has actually exercised `/checkin` or the restored RSVP flow against a live Calendar yet. |
